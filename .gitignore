.claude
.screenshots

# Secrets — never commit (root .env only; service <name>/.env files are safe to commit)
/.env

# Bind-mount data directories — large runtime state, not source-controlled
paperless/data/
paperless/media/
paperless/export/
paperless/consume/
postgres/data/
redis/data/
paperless-ai-next/data/
open-webui/data/
ollama/data/

# Migration export staging area (may be large)
migration-export/

# Old pre-compose runtime state
paperless-ai-data/
export/
consume/

# Dashboard + QuestDB runtime data
questdb/data/
apps/dashboard/.next/
apps/dashboard/node_modules/
node_modules/
